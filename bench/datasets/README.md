# Dataset Assets

This directory is reserved for benchmark dataset notes and future checked-in
benchmark-only assets.

The current benchmark harness reads dataset metadata from
`bench/manifests/datasets/` and points at the existing repository map files.
