# Query Assets

This directory is reserved for benchmark query notes and future checked-in
benchmark-only query assets.

The current benchmark harness reads query-set metadata from
`bench/manifests/query_sets/` and can wrap existing `scen/*.json` sources in a
clean manifest format.
